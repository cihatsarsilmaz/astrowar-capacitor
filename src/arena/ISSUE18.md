# Issue 18 incremental

Durum 2026-09-22 01:14 TR: Issue ACIK. Kapatma.

Main: `4cd114b` helpers import + ArenaView HUD.
fmt/labDisc/storageCap govde kopyasi yok. production JSX te.

Bu tur (kuyruk 3): `battle/resolve` `mode=arena` receipt.
- functions/index.js arena dalı (auth + validate + arenaReceipts)
- src/arena/receipt.js local receipt + optional POST
- ArenaView mac bitince receipt satiri
- Idle fleet battle() ayni kalir

Issue govdesi hâlâ eski snippet + `@Collaborators addpeople` basligi.
Playtest: `npm run dev ?mode=arena`. Test dosyasina dokunulmadi.

## Sonraki tek adim (kuyruk 4)
Q6 — playtest notu (`npm run dev ?mode=arena`).
