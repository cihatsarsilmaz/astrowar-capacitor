# Issue 18 incremental

Durum 2026-09-22 02:16 TR: Issue ACIK. Kapatma.

Main: helpers import + ArenaView HUD + receipt + Q6 notu.
fmt/labDisc/storageCap govde kopyasi yok. production JSX te.

Bu tur (kuyruk 4): Q6 playtest notu.
- `src/arena/Q6.md` protokol
- Hedef p50 < 100ms, p95 <= 300ms, n >= 30
- ArenaView Q6 satiri / auto x30 zaten var
- Telefon p95 olcumu yok — bilinmiyor

Issue govdesi hâlâ eski snippet + `@Collaborators addpeople` basligi.
Playtest: `npm run dev` sonra `/?mode=arena`. Test dosyasina dokunulmadi.

## Sonraki tek adim (kuyruk 5)
8 birimin kalan 3u (interceptor, reaper, corebreak) sim e.
