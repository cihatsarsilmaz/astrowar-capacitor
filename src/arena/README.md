# Arena (Faz 1)

Playtest: `npm run dev` sonra `/?mode=arena`
Q6 protokol: `src/arena/Q6.md` — p50 < 100ms, p95 <= 300ms, n >= 30
Headless Q6: `npm run arena:q6` (sim.place CPU; telefon degil)
Headless mac: `node src/arena/sim.js`

Idle oyun `/?` ile ayni kalir.
