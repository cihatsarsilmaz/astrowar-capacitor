# Q6 playtest notu — AstrogameWAR Arena

Hedef (touch-to-paint, telefon): yerlestirme gecikmesi p50 < 100ms, p95 <= 300ms, n >= 30.
Kod: `src/arena/q6.js` + ArenaView `Q6 touch` satiri / `touch kayit` + `Q6 auto x30` + `playtest.js`.

Headless `sim.place` ve `Q6 auto` telefon sayilmaz. GECTI iddiasi yok.

## Nasil (telefon)

```
npm run dev
```

Telefonda: `http://<pc-ip>:5173/?mode=arena`

1. Kart sec.
2. Parmakla alt yariya bas (mouse `Q6 touch` sayilmaz).
3. n >= 30 touch.
4. `touch kayit` — rapor panoya; yapistir.
5. `sifirla` log + rapor temizler.

## Nasil (headless)

```
npm run arena:q6
```

## Kayit 2026-09-22 07:26 TR

| Kaynak | Cihaz | n | p50 | p95 | flag |
|---|---|---|---|---|---|
| sim.place | headless-node | 30 | 0ms | 0ms | HEADLESS — telefon degil |
| Q6 auto x30 | tarayici | - | - | - | bilinmiyor |
| touch | telefon | - | - | - | bilinmiyor — kullanici raporu yok |

Idle `/?` ayni kalir. Issue 18 acik. Play Store yok.
Sonraki kuyruk: Issue 18 basligini `v11: helpers.js tek kaynak` yap (kapatma).
